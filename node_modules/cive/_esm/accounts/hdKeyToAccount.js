import { toHex } from 'viem/utils';
import { privateKeyToAccount } from './privateKeyToAccount.js';
/**
 * @description Creates an Account from a HD Key.
 *
 * @returns A HD Account.
 */
export function hdKeyToAccount(hdKey_, { networkId, accountIndex = 0, addressIndex = 0, changeIndex = 0, path, verbose = false, }) {
    // default Conflux path-503
    const hdKey = hdKey_.derive(path || `m/44'/503'/${accountIndex}'/${changeIndex}/${addressIndex}`);
    const account = privateKeyToAccount(toHex(hdKey.privateKey), {
        networkId,
        verbose,
    });
    return {
        ...account,
        getHdKey: () => hdKey,
        source: 'hd',
    };
}
//# sourceMappingURL=hdKeyToAccount.js.map