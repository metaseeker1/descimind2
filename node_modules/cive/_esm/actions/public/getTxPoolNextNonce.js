export async function getTxPoolNextNonce(client, { address }) {
    const nonce = await client.request({
        method: 'txpool_nextNonce',
        params: [address],
    });
    return Number(nonce);
}
//# sourceMappingURL=getTxPoolNextNonce.js.map