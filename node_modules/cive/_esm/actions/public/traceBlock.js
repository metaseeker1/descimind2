import { BlockNotFoundError } from '../../errors/block.js';
import { formatTraceBlock } from '../../utils/formatters/tract.js';
export async function traceBlock(client, { blockHash }) {
    const result = await client.request({
        method: 'trace_block',
        params: [blockHash],
    });
    if (!result)
        throw new BlockNotFoundError({ blockHash });
    return formatTraceBlock(result);
}
//# sourceMappingURL=traceBlock.js.map