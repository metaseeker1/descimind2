import { numberToHex } from 'viem/utils';
import { formatPosRewards } from '../../utils/formatters/pos.js';
export async function getPoSRewards(client, { epochNumber }) {
    const result = await client.request({
        method: 'pos_getRewardsByEpoch',
        params: [numberToHex(epochNumber)],
    });
    if (!result)
        return null;
    return formatPosRewards(result);
}
//# sourceMappingURL=getPoSRewards.js.map