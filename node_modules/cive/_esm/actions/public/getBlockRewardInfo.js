import { numberToHex } from 'viem/utils';
import { formatReward } from '../../utils/formatters/reward.js';
export async function getBlockRewardInfo(client, { epochNumber, epochTag = 'latest_checkpoint', } = {}) {
    const _epochNumber = epochNumber ? numberToHex(epochNumber) : undefined;
    const result = await client.request({
        method: 'cfx_getBlockRewardInfo',
        params: [_epochNumber || epochTag],
    });
    return result.map(formatReward);
}
//# sourceMappingURL=getBlockRewardInfo.js.map