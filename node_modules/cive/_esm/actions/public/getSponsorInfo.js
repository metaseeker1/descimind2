import { numberToHex } from 'viem/utils';
import { formatSponsor } from '../../utils/formatters/sponsor.js';
export async function GetSponsorInfo(client, { address, epochNumber, epochTag = 'latest_state' }) {
    const _epochNumber = epochNumber ? numberToHex(epochNumber) : undefined;
    const data = await client.request({
        method: 'cfx_getSponsorInfo',
        params: [address, _epochNumber || epochTag],
    });
    return formatSponsor(data);
}
//# sourceMappingURL=getSponsorInfo.js.map