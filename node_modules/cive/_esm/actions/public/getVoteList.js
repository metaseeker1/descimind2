import { numberToHex } from 'viem/utils';
import { formatVote } from '../../utils/formatters/vote.js';
export async function getVoteList(client, { address, epochNumber, epochTag = 'latest_state' }) {
    const _epochNumber = epochNumber ? numberToHex(epochNumber) : undefined;
    const result = await client.request({
        method: 'cfx_getVoteList',
        params: [address, _epochNumber || epochTag],
    });
    return result.map(formatVote);
}
//# sourceMappingURL=getVoteList.js.map