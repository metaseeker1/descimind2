import { numberToHex } from 'viem/utils';
export async function GetStorageAt(client, { address, slot, epochNumber, epochTag = 'latest_state', }) {
    const _epochNumber = epochNumber ? numberToHex(epochNumber) : undefined;
    const data = await client.request({
        method: 'cfx_getStorageAt',
        params: [address, slot, _epochNumber || epochTag],
    });
    if (data === '0x')
        return null;
    return data;
}
//# sourceMappingURL=getStorageAt.js.map