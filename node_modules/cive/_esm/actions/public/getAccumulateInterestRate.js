import { numberToHex } from 'viem/utils';
export async function getAccumulateInterestRate(client, { epochNumber, epochTag = 'latest_state', } = {}) {
    const _epochNumber = epochNumber ? numberToHex(epochNumber) : undefined;
    const result = await client.request({
        method: 'cfx_getAccumulateInterestRate',
        params: [_epochNumber || epochTag],
    });
    return BigInt(result);
}
//# sourceMappingURL=getAccumulateInterestRate.js.map