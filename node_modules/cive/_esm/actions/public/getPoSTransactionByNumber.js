import { numberToHex } from 'viem/utils';
import { formatPoSTransaction } from '../../utils/formatters/pos.js';
export async function getPoSTransactionByNumber(client, { transactionNumber }) {
    const result = await client.request({
        method: 'pos_getTransactionByNumber',
        params: [numberToHex(transactionNumber)],
    });
    if (!result)
        return null;
    return formatPoSTransaction(result);
}
//# sourceMappingURL=getPoSTransactionByNumber.js.map