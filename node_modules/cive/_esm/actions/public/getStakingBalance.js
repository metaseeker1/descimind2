import { numberToHex } from 'viem/utils';
export async function getStakingBalance(client, { address, epochNumber, epochTag = 'latest_state', }) {
    const _epochNumber = epochNumber ? numberToHex(epochNumber) : undefined;
    const balance = await client.request({
        method: 'cfx_getStakingBalance',
        params: [address, _epochNumber || epochTag],
    });
    return BigInt(balance);
}
//# sourceMappingURL=getStakingBalance.js.map