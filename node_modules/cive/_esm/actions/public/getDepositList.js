import { numberToHex } from 'viem/utils';
import { formatDeposit } from '../../utils/formatters/deposit.js';
export async function getDepositList(client, { address, epochNumber, epochTag = 'latest_state' }) {
    const _epochNumber = epochNumber ? numberToHex(epochNumber) : undefined;
    const result = await client.request({
        method: 'cfx_getDepositList',
        params: [address, _epochNumber || epochTag],
    });
    return result.map(formatDeposit);
}
//# sourceMappingURL=getDepositList.js.map