import { numberToHex } from 'viem/utils';
export async function getAdmin(client, { address, epochNumber, epochTag = 'latest_state' }) {
    const _epochNumber = epochNumber ? numberToHex(epochNumber) : undefined;
    const adminAddress = await client.request({
        method: 'cfx_getAdmin',
        params: [address, _epochNumber || epochTag],
    });
    return adminAddress;
}
//# sourceMappingURL=getAdmin.js.map