import { numberToHex } from 'viem/utils';
export async function unlockLocalNodeAccount(client, { address, password, duration = 300 }) {
    const duration_ = numberToHex(duration);
    const result = await client.request({
        method: 'unlock_account',
        params: [address, password, duration_],
    });
    return result;
}
//# sourceMappingURL=unlockLocalNodeAccount.js.map