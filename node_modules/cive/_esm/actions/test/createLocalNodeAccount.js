export async function createLocalNodeAccount(client, { password }) {
    const address = await client.request({
        method: 'new_account',
        params: [password],
    });
    return address;
}
//# sourceMappingURL=createLocalNodeAccount.js.map