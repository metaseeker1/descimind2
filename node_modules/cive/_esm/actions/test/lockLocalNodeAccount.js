export async function lockLocalNodeAccount(client, { address }) {
    const result = await client.request({
        method: 'lock_account',
        params: [address],
    });
    return result;
}
//# sourceMappingURL=lockLocalNodeAccount.js.map