import { secp256k1 } from '@noble/curves/secp256k1';
import { hexToBigInt, toHex, } from 'viem';
export function serializeSignature({ r, s, v }) {
    return `0x${new secp256k1.Signature(hexToBigInt(r), hexToBigInt(s)).toCompactHex()}${toHex(v, { size: 1 }).slice(2)}`;
}
//# sourceMappingURL=serializeSignature.js.map