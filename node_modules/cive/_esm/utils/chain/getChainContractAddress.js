import { ChainDoesNotSupportContract, } from '../errors/chain.js';
export function getChainContractAddress({ epochNumber, chain, contract: name, }) {
    const contract = chain?.contracts?.[name];
    if (!contract)
        throw new ChainDoesNotSupportContract({
            chain,
            contract: { name },
        });
    if (epochNumber &&
        contract.epochCreated &&
        contract.epochCreated > epochNumber)
        throw new ChainDoesNotSupportContract({
            epochNumber,
            chain,
            contract: {
                name,
                blockCreated: contract.epochCreated,
            },
        });
    return contract.address;
}
//# sourceMappingURL=getChainContractAddress.js.map