"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getPoSRewardByEpoch = getPoSRewardByEpoch;
const utils_1 = require("viem/utils");
async function getPoSRewardByEpoch(client, { epochNumber }) {
    const result = await client.request({
        method: 'cfx_getPoSRewardByEpoch',
        params: [(0, utils_1.numberToHex)(epochNumber)],
    });
    if (result === null)
        return undefined;
    return {
        ...result,
        accountRewards: result.accountRewards.map((item) => ({
            ...item,
            reward: BigInt(item.reward),
        })),
    };
}
//# sourceMappingURL=getPoSRewardByEpoch.js.map