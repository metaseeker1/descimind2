"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getPoSEconomics = getPoSEconomics;
const utils_1 = require("viem/utils");
async function getPoSEconomics(client, { epochNumber, epochTag = 'latest_state' } = {}) {
    const _epochNumber = epochNumber ? (0, utils_1.numberToHex)(epochNumber) : undefined;
    const result = await client.request({
        method: 'cfx_getPoSEconomics',
        params: [_epochNumber || epochTag],
    });
    return {
        ...result,
        distributablePosInterest: BigInt(result.distributablePosInterest),
        lastDistributeBlock: BigInt(result.lastDistributeBlock),
        totalPosStakingTokens: BigInt(result.totalPosStakingTokens),
    };
}
//# sourceMappingURL=getPoSEconomics.js.map