"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.GetStorageRoot = GetStorageRoot;
const utils_1 = require("viem/utils");
async function GetStorageRoot(client, { address, epochNumber, epochTag = 'latest_state' }) {
    const _epochNumber = epochNumber ? (0, utils_1.numberToHex)(epochNumber) : undefined;
    const data = await client.request({
        method: 'cfx_getStorageRoot',
        params: [address, _epochNumber || epochTag],
    });
    return data;
}
//# sourceMappingURL=getStorageRoot.js.map