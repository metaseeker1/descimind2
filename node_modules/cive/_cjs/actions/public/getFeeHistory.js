"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getFeeHistory = getFeeHistory;
const utils_1 = require("viem/utils");
const feeHistory_js_1 = require("../../utils/formatters/feeHistory.js");
async function getFeeHistory(client, { epochCount: blockCount, epochNumber, epochTag = 'latest_state', rewardPercentiles, }) {
    const epochNumberHex = epochNumber ? (0, utils_1.numberToHex)(epochNumber) : undefined;
    const feeHistory = await client.request({
        method: 'cfx_feeHistory',
        params: [
            (0, utils_1.numberToHex)(blockCount),
            epochNumberHex || epochTag,
            rewardPercentiles,
        ],
    }, { dedupe: Boolean(epochNumberHex) });
    return (0, feeHistory_js_1.formatFeeHistory)(feeHistory);
}
//# sourceMappingURL=getFeeHistory.js.map