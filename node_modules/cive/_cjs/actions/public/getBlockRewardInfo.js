"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getBlockRewardInfo = getBlockRewardInfo;
const utils_1 = require("viem/utils");
const reward_js_1 = require("../../utils/formatters/reward.js");
async function getBlockRewardInfo(client, { epochNumber, epochTag = 'latest_checkpoint', } = {}) {
    const _epochNumber = epochNumber ? (0, utils_1.numberToHex)(epochNumber) : undefined;
    const result = await client.request({
        method: 'cfx_getBlockRewardInfo',
        params: [_epochNumber || epochTag],
    });
    return result.map(reward_js_1.formatReward);
}
//# sourceMappingURL=getBlockRewardInfo.js.map