"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getVoteList = getVoteList;
const utils_1 = require("viem/utils");
const vote_js_1 = require("../../utils/formatters/vote.js");
async function getVoteList(client, { address, epochNumber, epochTag = 'latest_state' }) {
    const _epochNumber = epochNumber ? (0, utils_1.numberToHex)(epochNumber) : undefined;
    const result = await client.request({
        method: 'cfx_getVoteList',
        params: [address, _epochNumber || epochTag],
    });
    return result.map(vote_js_1.formatVote);
}
//# sourceMappingURL=getVoteList.js.map