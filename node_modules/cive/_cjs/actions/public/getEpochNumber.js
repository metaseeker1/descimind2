"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getEpochNumber = getEpochNumber;
const withCache_js_1 = require("../../utils/promise/withCache.js");
const cacheKey = (id, tag) => `blockNumber.${id}.${tag}`;
async function getEpochNumber(client, { epochTag = 'latest_mined', cacheTime = client.cacheTime, } = {}) {
    const epochNumberHex = await (0, withCache_js_1.withCache)(() => client.request({
        method: 'cfx_epochNumber',
        params: [epochTag],
    }), { cacheKey: cacheKey(client.uid, epochTag), cacheTime });
    return BigInt(epochNumberHex);
}
//# sourceMappingURL=getEpochNumber.js.map