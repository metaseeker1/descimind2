"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getStakingBalance = getStakingBalance;
const utils_1 = require("viem/utils");
async function getStakingBalance(client, { address, epochNumber, epochTag = 'latest_state', }) {
    const _epochNumber = epochNumber ? (0, utils_1.numberToHex)(epochNumber) : undefined;
    const balance = await client.request({
        method: 'cfx_getStakingBalance',
        params: [address, _epochNumber || epochTag],
    });
    return BigInt(balance);
}
//# sourceMappingURL=getStakingBalance.js.map