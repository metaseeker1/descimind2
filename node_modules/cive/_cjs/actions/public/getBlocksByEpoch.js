"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getBlocksByEpoch = getBlocksByEpoch;
const viem_1 = require("viem");
async function getBlocksByEpoch(client, { epochNumber, epochTag = 'latest_state' } = {}) {
    const _epochNumber = epochNumber ? (0, viem_1.numberToHex)(epochNumber) : undefined;
    const blockHashes = await client.request({
        method: 'cfx_getBlocksByEpoch',
        params: [_epochNumber || epochTag],
    });
    return blockHashes;
}
//# sourceMappingURL=getBlocksByEpoch.js.map