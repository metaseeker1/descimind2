"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getTransaction = getTransaction;
const viem_1 = require("viem");
const transaction_js_1 = require("../../utils/formatters/transaction.js");
async function getTransaction(client, { hash }) {
    const transaction = await client.request({
        method: 'cfx_getTransactionByHash',
        params: [hash],
    });
    if (!transaction) {
        throw new viem_1.TransactionNotFoundError({
            hash,
        });
    }
    const format = client.chain?.formatters?.transaction?.format || transaction_js_1.formatTransaction;
    return format(transaction);
}
//# sourceMappingURL=getTransaction.js.map