"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.checkBalanceAgainstTransaction = checkBalanceAgainstTransaction;
const utils_1 = require("viem/utils");
async function checkBalanceAgainstTransaction(client, { accountAddress, contractAddress, gasLimit, gasPrice, storageLimit, epochTag = 'latest_state', epochNumber, }) {
    const _epochNumber = epochNumber ? (0, utils_1.numberToHex)(epochNumber) : undefined;
    const result = await client.request({
        method: 'cfx_checkBalanceAgainstTransaction',
        params: [
            accountAddress,
            contractAddress,
            (0, utils_1.numberToHex)(gasLimit),
            (0, utils_1.numberToHex)(gasPrice),
            (0, utils_1.numberToHex)(storageLimit),
            _epochNumber || epochTag,
        ],
    });
    return result;
}
//# sourceMappingURL=checkBalanceAgainstTransaction.js.map