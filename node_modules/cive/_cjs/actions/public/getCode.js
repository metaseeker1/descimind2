"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getCode = getCode;
const utils_1 = require("viem/utils");
async function getCode(client, { address, epochNumber, epochTag = 'latest_state' }) {
    const _epochNumber = epochNumber ? (0, utils_1.numberToHex)(epochNumber) : undefined;
    const hex = await client.request({
        method: 'cfx_getCode',
        params: [address, _epochNumber || epochTag],
    });
    if (hex === '0x')
        return undefined;
    return hex;
}
//# sourceMappingURL=getCode.js.map