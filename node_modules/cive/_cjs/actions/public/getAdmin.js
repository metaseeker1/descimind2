"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getAdmin = getAdmin;
const utils_1 = require("viem/utils");
async function getAdmin(client, { address, epochNumber, epochTag = 'latest_state' }) {
    const _epochNumber = epochNumber ? (0, utils_1.numberToHex)(epochNumber) : undefined;
    const adminAddress = await client.request({
        method: 'cfx_getAdmin',
        params: [address, _epochNumber || epochTag],
    });
    return adminAddress;
}
//# sourceMappingURL=getAdmin.js.map