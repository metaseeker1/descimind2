"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getCollateralInfo = getCollateralInfo;
const utils_1 = require("viem/utils");
async function getCollateralInfo(client, { epochNumber, epochTag = 'latest_state' } = {}) {
    const _epochNumber = epochNumber ? (0, utils_1.numberToHex)(epochNumber) : undefined;
    const epoch = _epochNumber || epochTag;
    const result = await client.request({
        method: 'cfx_getCollateralInfo',
        params: [epoch],
    });
    return {
        ...result,
        totalStorageTokens: BigInt(result.totalStorageTokens),
        convertedStoragePoints: BigInt(result.convertedStoragePoints),
        usedStoragePoints: BigInt(result.usedStoragePoints),
    };
}
//# sourceMappingURL=getCollateralInfo.js.map