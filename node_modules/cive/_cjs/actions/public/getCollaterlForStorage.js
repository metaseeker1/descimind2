"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getCollateralForStorage = getCollateralForStorage;
const utils_1 = require("viem/utils");
async function getCollateralForStorage(client, { address, epochNumber, epochTag = 'latest_state', }) {
    const _epochNumber = epochNumber ? (0, utils_1.numberToHex)(epochNumber) : undefined;
    const collateralForStorage = await client.request({
        method: 'cfx_getCollateralForStorage',
        params: [address, _epochNumber || epochTag],
    });
    return BigInt(collateralForStorage);
}
//# sourceMappingURL=getCollaterlForStorage.js.map