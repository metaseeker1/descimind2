"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getInterestRate = getInterestRate;
const utils_1 = require("viem/utils");
async function getInterestRate(client, { epochNumber, epochTag = 'latest_state' } = {}) {
    const _epochNumber = epochNumber ? (0, utils_1.numberToHex)(epochNumber) : undefined;
    const result = await client.request({
        method: 'cfx_getInterestRate',
        params: [_epochNumber || epochTag],
    });
    return BigInt(result);
}
//# sourceMappingURL=getInterestRate.js.map