"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getParamsFromVote = getParamsFromVote;
const utils_1 = require("viem/utils");
async function getParamsFromVote(client, { epochNumber, epochTag = 'latest_state' } = {}) {
    const _epochNumber = epochNumber ? (0, utils_1.numberToHex)(epochNumber) : undefined;
    const result = await client.request({
        method: 'cfx_getParamsFromVote',
        params: [_epochNumber || epochTag],
    });
    return {
        ...result,
        powBaseReward: BigInt(result.powBaseReward),
        interestRate: BigInt(result.interestRate),
        storagePointProp: BigInt(result.storagePointProp),
        baseFeeShareProp: BigInt(result.baseFeeShareProp),
    };
}
//# sourceMappingURL=getParamsFromVote.js.map