"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.GetStorageAt = GetStorageAt;
const utils_1 = require("viem/utils");
async function GetStorageAt(client, { address, slot, epochNumber, epochTag = 'latest_state', }) {
    const _epochNumber = epochNumber ? (0, utils_1.numberToHex)(epochNumber) : undefined;
    const data = await client.request({
        method: 'cfx_getStorageAt',
        params: [address, slot, _epochNumber || epochTag],
    });
    if (data === '0x')
        return null;
    return data;
}
//# sourceMappingURL=getStorageAt.js.map