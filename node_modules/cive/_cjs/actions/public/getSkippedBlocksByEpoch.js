"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getSkippedBlocksByEpoch = getSkippedBlocksByEpoch;
const utils_1 = require("viem/utils");
const block_js_1 = require("../../utils/formatters/block.js");
async function getSkippedBlocksByEpoch(client, { epochNumber, epochTag = 'latest_state', } = {}) {
    const _epochNumber = epochNumber ? (0, utils_1.numberToHex)(epochNumber) : undefined;
    const blocks = await client.request({
        method: 'cfx_getSkippedBlocksByEpoch',
        params: [_epochNumber || epochTag],
    });
    const format = client.chain?.formatters?.block?.format || block_js_1.formatBlock;
    return blocks.map((block) => format(block));
}
//# sourceMappingURL=getSkippedBlocksByEpoch.js.map