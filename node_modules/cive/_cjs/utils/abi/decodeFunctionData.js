"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.decodeFunctionData = decodeFunctionData;
const viem_1 = require("viem");
const utils_1 = require("viem/utils");
const decodeAbiParameters_js_1 = require("./decodeAbiParameters.js");
function decodeFunctionData(parameters) {
    const { abi, data, networkId } = parameters;
    const signature = (0, viem_1.slice)(data, 0, 4);
    const description = abi.find((x) => x.type === 'function' &&
        signature === (0, viem_1.toFunctionSelector)((0, utils_1.formatAbiItem)(x)));
    if (!description)
        throw new viem_1.AbiFunctionSignatureNotFoundError(signature, {
            docsPath: '/docs/contract/decodeFunctionData',
        });
    return {
        functionName: description.name,
        args: ('inputs' in description &&
            description.inputs &&
            description.inputs.length > 0
            ? (0, decodeAbiParameters_js_1.decodeAbiParameters)(description.inputs, (0, viem_1.slice)(data, 4), networkId)
            : undefined),
    };
}
//# sourceMappingURL=decodeFunctionData.js.map