"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.decodeDeployData = decodeDeployData;
const viem_1 = require("viem");
const decodeAbiParameters_js_1 = require("./decodeAbiParameters.js");
const docsPath = '/docs/contract/decodeDeployData';
function decodeDeployData(parameters) {
    const { abi, bytecode, data, networkId } = parameters;
    if (data === bytecode)
        return { bytecode };
    const description = abi.find((x) => 'type' in x && x.type === 'constructor');
    if (!description)
        throw new viem_1.AbiConstructorNotFoundError({ docsPath });
    if (!('inputs' in description))
        throw new viem_1.AbiConstructorParamsNotFoundError({ docsPath });
    if (!description.inputs || description.inputs.length === 0)
        throw new viem_1.AbiConstructorParamsNotFoundError({ docsPath });
    const args = (0, decodeAbiParameters_js_1.decodeAbiParameters)(description.inputs, `0x${data.replace(bytecode, '')}`, networkId);
    return { args, bytecode };
}
//# sourceMappingURL=decodeDeployData.js.map