"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildRequest = buildRequest;
exports.shouldRetry = shouldRetry;
const viem_1 = require("viem");
const utils_1 = require("viem/utils");
const withDedupe_js_1 = require("./promise/withDedupe.js");
const withRetry_js_1 = require("./promise/withRetry.js");
function buildRequest(request, options = {}) {
    return async (args, overrideOptions = {}) => {
        const { dedupe = false, retryDelay = 150, retryCount = 3, uid, } = {
            ...options,
            ...overrideOptions,
        };
        const requestId = dedupe
            ? (0, utils_1.keccak256)((0, utils_1.stringToHex)(`${uid}.${(0, utils_1.stringify)(args)}`))
            : undefined;
        return (0, withDedupe_js_1.withDedupe)(() => (0, withRetry_js_1.withRetry)(async () => {
            try {
                return await request(args);
            }
            catch (err_) {
                const err = err_;
                switch (err.code) {
                    case viem_1.ParseRpcError.code:
                        throw new viem_1.ParseRpcError(err);
                    case viem_1.InvalidRequestRpcError.code:
                        throw new viem_1.InvalidRequestRpcError(err);
                    case viem_1.MethodNotFoundRpcError.code:
                        throw new viem_1.MethodNotFoundRpcError(err, { method: args.method });
                    case viem_1.InvalidParamsRpcError.code:
                        throw new viem_1.InvalidParamsRpcError(err);
                    case viem_1.InternalRpcError.code:
                        throw new viem_1.InternalRpcError(err);
                    case viem_1.InvalidInputRpcError.code:
                        throw new viem_1.InvalidInputRpcError(err);
                    case viem_1.ResourceNotFoundRpcError.code:
                        throw new viem_1.ResourceNotFoundRpcError(err);
                    case viem_1.ResourceUnavailableRpcError.code:
                        throw new viem_1.ResourceUnavailableRpcError(err);
                    case viem_1.TransactionRejectedRpcError.code:
                        throw new viem_1.TransactionRejectedRpcError(err);
                    case viem_1.MethodNotSupportedRpcError.code:
                        throw new viem_1.MethodNotSupportedRpcError(err, {
                            method: args.method,
                        });
                    case viem_1.LimitExceededRpcError.code:
                        throw new viem_1.LimitExceededRpcError(err);
                    case viem_1.JsonRpcVersionUnsupportedError.code:
                        throw new viem_1.JsonRpcVersionUnsupportedError(err);
                    case viem_1.UserRejectedRequestError.code:
                        throw new viem_1.UserRejectedRequestError(err);
                    case viem_1.UnauthorizedProviderError.code:
                        throw new viem_1.UnauthorizedProviderError(err);
                    case viem_1.UnsupportedProviderMethodError.code:
                        throw new viem_1.UnsupportedProviderMethodError(err);
                    case viem_1.ProviderDisconnectedError.code:
                        throw new viem_1.ProviderDisconnectedError(err);
                    case viem_1.ChainDisconnectedError.code:
                        throw new viem_1.ChainDisconnectedError(err);
                    case viem_1.SwitchChainError.code:
                        throw new viem_1.SwitchChainError(err);
                    case 5000:
                        throw new viem_1.UserRejectedRequestError(err);
                    default:
                        if (err_ instanceof viem_1.BaseError)
                            throw err_;
                        throw new viem_1.UnknownRpcError(err);
                }
            }
        }, {
            delay: ({ count, error }) => {
                if (error && error instanceof viem_1.HttpRequestError) {
                    const retryAfter = error?.headers?.get('Retry-After');
                    if (retryAfter?.match(/\d/))
                        return Number.parseInt(retryAfter) * 1000;
                }
                return ~~(1 << count) * retryDelay;
            },
            retryCount,
            shouldRetry: ({ error }) => shouldRetry(error),
        }), { enabled: dedupe, id: requestId });
    };
}
function shouldRetry(error) {
    if ('code' in error && typeof error.code === 'number') {
        if (error.code === -1)
            return true;
        if (error.code === viem_1.LimitExceededRpcError.code)
            return true;
        if (error.code === viem_1.InternalRpcError.code)
            return true;
        return false;
    }
    if (error instanceof viem_1.HttpRequestError && error.status) {
        if (error.status === 403)
            return true;
        if (error.status === 408)
            return true;
        if (error.status === 413)
            return true;
        if (error.status === 429)
            return true;
        if (error.status === 500)
            return true;
        if (error.status === 502)
            return true;
        if (error.status === 503)
            return true;
        if (error.status === 504)
            return true;
        return false;
    }
    return true;
}
//# sourceMappingURL=buildRequest.js.map