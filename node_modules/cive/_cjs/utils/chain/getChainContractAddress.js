"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getChainContractAddress = getChainContractAddress;
const chain_js_1 = require("../errors/chain.js");
function getChainContractAddress({ epochNumber, chain, contract: name, }) {
    const contract = chain?.contracts?.[name];
    if (!contract)
        throw new chain_js_1.ChainDoesNotSupportContract({
            chain,
            contract: { name },
        });
    if (epochNumber &&
        contract.epochCreated &&
        contract.epochCreated > epochNumber)
        throw new chain_js_1.ChainDoesNotSupportContract({
            epochNumber,
            chain,
            contract: {
                name,
                blockCreated: contract.epochCreated,
            },
        });
    return contract.address;
}
//# sourceMappingURL=getChainContractAddress.js.map