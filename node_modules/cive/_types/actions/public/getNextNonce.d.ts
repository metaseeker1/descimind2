import type { NumberToHexErrorType, RequestErrorType } from 'viem/utils';
import type { Address } from '../../accounts/types.js';
import type { Client } from '../../clients/createClient.js';
import type { Transport } from '../../clients/transports/createTransport.js';
import type { ErrorType } from '../../errors/utils.js';
import type { EpochNumber, EpochTag } from '../../types/block.js';
import type { Chain } from '../../types/chain.js';
export type GetNextNonceParameters = {
    address: Address;
    /**
     * @default true
     */
    tryTxPool?: boolean;
} & ({
    /**
     * @default 'latest_state'
     */
    epochTag?: Exclude<EpochTag, 'latest_finalized' | 'latest_mined'> | undefined;
    epochNumber?: never | undefined;
} | {
    epochTag?: never | undefined;
    epochNumber?: EpochNumber | undefined;
});
export type GetNextNonceReturnType = number;
export type GetNextNonceErrorType = RequestErrorType | NumberToHexErrorType | ErrorType;
export declare function getNextNonce<TChain extends Chain | undefined>(client: Client<Transport, TChain>, { address, epochNumber, tryTxPool, epochTag, }: GetNextNonceParameters): Promise<GetNextNonceReturnType>;
//# sourceMappingURL=getNextNonce.d.ts.map